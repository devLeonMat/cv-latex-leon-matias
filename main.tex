%% ============================================================
%%  main.tex — Roslin Leon Matias · Lead Software Engineer CV
%%  Engine: XeLaTeX  |  Target: 1 page  |  Lang: EN
%% ============================================================

\documentclass[10pt, letterpaper]{article}

%% ── Packages ────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{fontawesome5}
\usepackage{titlesec}
\usepackage{multicol}
\usepackage{array}
\usepackage{tabularx}
\usepackage{parskip}
\usepackage{microtype}
\usepackage{setspace}

%% ── Page geometry ───────────────────────────────────────────
\geometry{
  top=0.45in,
  bottom=0.45in,
  left=0.55in,
  right=0.55in
}

%% ── Color palette ───────────────────────────────────────────
\definecolor{primary}{HTML}{4F46E5}      % indigo-600
\definecolor{primarydark}{HTML}{312E81}  % indigo-900
\definecolor{accent}{HTML}{7C3AED}       % violet-600
\definecolor{textmain}{HTML}{111827}     % gray-900
\definecolor{textmuted}{HTML}{6B7280}    % gray-500
\definecolor{rulecol}{HTML}{E5E7EB}      % gray-200
\definecolor{linkcolor}{HTML}{4F46E5}    % same as primary

%% ── Hyperref ────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  urlcolor=linkcolor,
  pdfauthor={Roslin Leon Matias},
  pdftitle={Roslin Leon Matias — CV},
  pdfsubject={Lead Software Engineer},
  pdfkeywords={Software Engineer, Java, Spring Boot, AWS, React, TypeScript}
}

%% ── Typography ──────────────────────────────────────────────
\renewcommand{\familydefault}{\sfdefault}
\color{textmain}

%% ── Section heading style ───────────────────────────────────
\titleformat{\section}
  {\large\bfseries\color{primarydark}}
  {}
  {0em}
  {}
  [\vspace{-6pt}\textcolor{primary}{\rule{\linewidth}{1.2pt}}\vspace{2pt}]

\titlespacing{\section}{0pt}{8pt}{4pt}

%% ── Custom commands ─────────────────────────────────────────

% Experience entry: \expentry{Title}{Company · Location}{Date range}
\newcommand{\expentry}[3]{%
  \textbf{#1} \hfill \textcolor{textmuted}{\small #3} \\
  \textcolor{primary}{\small\textit{#2}}
}

% Education entry: \eduentry{Degree}{Institution}{Year}
\newcommand{\eduentry}[3]{%
  \textbf{#1} \hfill \textcolor{textmuted}{\small #3} \\
  \textcolor{primary}{\small\textit{#2}}
}

% Compact bullet list
\newenvironment{cvitems}{%
  \begin{itemize}[leftmargin=1.2em, itemsep=0pt, topsep=2pt, parsep=0pt]
  \small
}{%
  \end{itemize}
}

%% ── Document ────────────────────────────────────────────────
\begin{document}
\pagestyle{empty}

%% ── HEADER ──────────────────────────────────────────────────
\input{sections/header}

\vspace{4pt}

%% ── SUMMARY ─────────────────────────────────────────────────
\input{sections/summary}

%% ── EXPERIENCE ──────────────────────────────────────────────
\input{sections/experience}

%% ── SKILLS ──────────────────────────────────────────────────
\input{sections/skills}

%% ── EDUCATION ───────────────────────────────────────────────
\input{sections/education}

\end{document}
